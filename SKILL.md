---
name: product-deconstruction
description: >-
  对互联网/AI产品进行系统性拆解分析，输出完整HTML图文报告（全屏流式风格）。
  当用户提到：产品分析、产品拆解、产品深挖、产品Review、竞品分析、Deep Dive、某个产品怎么样/有什么优缺点、帮我看一下XX产品、产品案例研究时，都应该触发此Skill。
  即使只是随口问一句"你觉得XX产品怎么样"，也要主动建议做完整的产品拆解分析。
---

# 产品拆解 Skill v3

## 核心理念

产品拆解不是罗列功能清单。你要回答的问题是：
- **这个产品为什么存在？**（JTBD）
- **它为什么能活下来/活不下去？**（商业模式+竞争）
- **如果我是PM，下一步该做什么？**（改进建议）

每个模块都要有**判断和结论**，不是描述。不要说"它有搜索功能"，要说"它的搜索入口很深，说明搜索不是这个产品的核心路径"。

---

## 资源文件（按需读取）

| 文件 | 什么时候读 |
|------|-----------|
| `references/search-channels.md` | Step 2 信息收集时，查搜索渠道和信息缺失处理规则 |
| `references/chart-catalog.md` | 输出报告时，查图型映射、选型流程、视觉语法、SVG 安全规则 |
| `references/reference-sites.md` | 写报告时，参考风格网站 |
| `templates/report-template.html` | 通用骨架参考（结构和组件名） |
| `templates/style-academic.html` | 学术数据风格模板（SaaS/增长/B2B） |
| `templates/style-dark.html` | 暗色科技风格模板（AI/开发者/加密） |
| `templates/style-enterprise.html` | 企业信任风格模板（金融/企业级/合规） |
| `templates/style-editorial.html` | 温暖编辑风格模板（消费/生活方式/D2C） |
| `templates/style-minimal.html` | 开发者极简风格模板（开源/CLI/API） |

---

## 执行流程

### Step 0: 产品分级（先于一切）

在开始分析之前，先判断产品复杂度，决定分析深度。不要对所有产品一视同仁。

```
S 级（全量分析）：用户过亿 or 年营收过亿 or 团队 100+ or 商业模式复杂
  → 六层全做，每个模块深入

A 级（标准分析）：用户百万级 or 有明确商业模式 or 团队 10-100
  → 六层全做，Layer 4-5 可精简

B 级（轻量分析）：用户十万级以下 or 个人/小团队项目 or 工具型产品
  → 跳过 Layer 4（数据埋点推断）和 Layer 5 运营策略的 C 部分
  → Layer 3 降级为定性判断

C 级（极简分析）：已关停 / 明显衰退 / 仅做存档记录
  → 只做 Layer 0 + Layer 1 + Layer 6 + 改进建议（或"为什么不值得做"）
```

分级依据（按优先级排序）：
1. 公开融资额 or 团队规模
2. App Store/Play Store 下载量或评分数量
3. 功能模块数（3 个以内 = 工具型 = B 级起步）
4. 是否有独立 App（仅网页/插件 = B 级起步）

### Step 0.5: 视觉风格决策

根据产品类型选择对应的视觉风格模板。**必须在输出 HTML 之前完成此步骤。**

| 产品类型 | 风格模板 | 主色 | 字体组合 | 布局特征 |
|---------|---------|------|---------|---------|
| SaaS / 增长工具 / B2B | `style-academic.html` | 深绿 #1a5f4a | Fraunces + Inter | 暗色 hero + 章节编号 + 来源标注 |
| AI / 开发者工具 / 加密 | `style-dark.html` | 电紫 #7c3aed | Space Grotesk + Mono | 无 hero + 深黑背景 + 紫色 glow |
| 金融 / 企业级 / 合规 | `style-enterprise.html` | 深蓝 #1e40af | IBM Plex + Sans | 白色 hero + 蓝色边框 + 全边框表格 |
| 消费 / 生活方式 / D2C | `style-editorial.html` | 焦橙 #c2410c | Lora + DM Sans | 暖色全屏 + 大留白 + 杂志滚动 |
| 开源 / CLI / API 产品 | `style-minimal.html` | 纯灰 #374151 | Mono + System UI | 无 hero + 纯白 + GitHub 风格 |

**决策规则**：
1. 读取对应风格模板的完整 HTML
2. 保留该模板的 CSS 变量、字体、布局骨架、组件样式
3. 替换所有内容数据为当前产品的分析结果
4. 如果产品类型不明确，默认使用 `style-academic.html`

**差异化要求**：每次生成的报告必须在视觉上与参考网站（Stripo 等）有明显区别。通过切换风格模板实现——不同产品类型自动产生不同视觉效果，避免所有报告看起来一模一样。

### Step 1: 产品生命阶段判断

| 阶段 | 特征 | 分析侧重 |
|------|------|---------|
| 探索期 | MVP、功能少、用户少、频繁迭代 | 验证方向是否对、PMF 信号 |
| 成长期 | 用户快速增长、功能扩张、开始变现 | 增长策略、留存机制、竞争壁垒 |
| 成熟期 | 用户稳定、功能完善、商业化充分 | 体验优化、效率提升、防御策略 |
| 衰退期 | 用户流失、更新缓慢、团队缩减 | 为什么失败、有什么教训 |

**如果产品已关停**：跳过 Layer 4（数据层）和 Layer 5（运营层），改为分析"死亡原因"和"可复用的教训"。

### Step 2: 信息收集（Layer 0）

联网搜索以下信息，标记信息来源。读取 `references/search-channels.md` 获取搜索渠道矩阵和信息缺失处理规则。

- 产品定位、一句话简介
- 目标用户 / 用户规模 / MAU/DAU
- 定价模式、付费结构
- 创始团队 / 背景 / 融资情况
- 最新版本/动态（近 3 个月）
- 用户评价（App Store 评分、知乎、Reddit、ProductHunt）
- 直接竞品（3-5 个，各用一句话概括差异）

### Step 3: 六层依次分析

根据 Step 0 的分级结果，决定哪些层需要深入、哪些可以精简或跳过。

---

## Layer 1: 战略层 —— 为什么做？

**核心问题：这个产品存在的前提是什么？**

- 产品目标与愿景（slogan vs 实际做的是否一致）
- 解决谁的什么问题？（用户画像 + 场景）
- 市场定位（领导者/挑战者/利基/补全）
- 核心 KPI 是啥？（GMV / DAU / 时长 / 转化率？）
- 判断：定位是否清晰？有没有漂移？

## Layer 2: 功能层 —— 做什么？

**核心问题：功能架构和核心流程是否合理？**

### A. 功能架构分析
- 产品功能清单（核心 → 辅助 → 运营，三层分类）
- 差异化功能 vs 基础功能
- 功能冗余分析：哪些功能使用率低但一直存在？

### B. 核心用户流程
画出从用户打开产品到完成核心任务的关键路径。
- 最短路径（理想体验，几步完成）
- 摩擦点（用户容易卡住的地方）
- 降低摩擦的设计（预填、默认值、磁吸等）

### C. 功能巧思拆解（只选 1-2 个最值得拆的核心功能）

**不要对每个功能都做完整分析。** 选最有代表性、最能体现产品设计思路的 1-2 个功能，用以下框架深入拆解：

```
1. JTBD 视角：
   - 用户"雇佣"这个功能完成什么任务？
   - 替代方案是什么？用户之前怎么做的？
   - 这个功能比替代方案好在哪？

2. 心理模型映射：
   - 功能隐喻来自什么现实场景？
   - 用户学习成本高吗？需要引导吗？
   - 有没有利用认知偏见？（默认选项、损失厌恶、社会认同）

3. 权衡判断（tradeoff）：
   - 这个功能最"聪明"的一处设计是什么？
   - 为什么竞品没这么做？
   - 这个设计牺牲了什么换来了什么？
```

其他核心功能用一句话概括其设计亮点即可，不需要展开。

## Layer 3: 体验层 —— 怎么做？

**核心问题：UI设计和交互体验是否支撑战略目标？**

### A. 视觉风格判断

**不需要精确取色。** 做定性判断即可：

- 整体配色倾向（深色/浅色/渐变？主色调给人什么感觉？）
- 排版风格（紧凑/宽松？信息密度高还是低？）
- 设计语言（Material Design / HIG / 自研 / 混搭？）
- 一致性判断（跨页面风格是否统一？有没有明显的"拼凑感"？）

**如果用户提供了截图**：可以补充具体的色彩分析（主色/辅色大致色相、对比度判断），但仍不做精确 hex 取值——那需要 CSS 检查器，模型做不到。

### B. 交互体验评估

- 功能可见性（用户知道这里可以操作吗？）
- 操作反馈（点击/滑动后多久有响应？什么形式？）
- 信息密度（一屏展示多少信息？有没有信息过载？）
- 可达性（核心操作几次点击完成？）
- 空状态设计（首次使用/无数据时展示什么？）

## Layer 4: 数据层 —— 怎么衡量？

**仅 S/A 级产品执行。B 级及以下跳过此层。**

**核心问题：产品团队用什么数据衡量功能好坏？**

### 指标体系假设

```
┌─ 北极星指标（产品成功的最核心指标）
│
├─ 一级指标（3-5个，反映产品健康度）
│  ├─ 活跃：DAU/MAU、日均使用时长
│  ├─ 转化：核心转化率（注册→付费）
│  ├─ 留存：次日/7日/30日留存
│  ├─ 营收：ARPU、LTV、GMV
│  └─ 质量：NPS、满意度评分
│
└─ 二级指标（具体功能级指标）
   ├─ 功能使用率
   ├─ 完成率（开始到完成的用户占比）
   └─ 时长分布
```

### 埋点方案推断

从产品行为反推可能的埋点设计。只列核心事件（3-5 个），不做完整 5W2H 推断——没有内部数据，推断太细就是在编。

## Layer 5: 运营层 —— 怎么增长？

**仅 S/A 级产品完整执行。B 级产品只做 AARRR 框架（不展开运营策略推断）。**

### AARRR 框架

| 环节 | 分析要点 | 关键指标 |
|------|---------|---------|
| 获客 Acquisition | 流量来源？付费/自然？ | CAC、渠道占比 |
| 激活 Activation | Aha Moment 在哪？多久到达？ | 激活率、TTV |
| 留存 Retention | 什么让用户回来？ | 次日/7日/30日留存 |
| 收入 Revenue | 谁付钱？付多少？怎么付？ | ARPU、LTV |
| 传播 Referral | 病毒系数K？分享场景？ | K-factor、分享率 |

### 增长飞轮（仅 S/A 级）

画出产品的"自增长循环"。例：更多用户 → 更多内容 → 更好的推荐 → 更高粘性 → 更多用户。

## Layer 6: 商业层 —— 怎么赚钱？

- **收入来源**：谁付钱？付多少？频次？
- **成本结构**：最大的成本是什么？
- **壁垒**：什么阻止竞品复制？
- **单位经济学**（估算）：LTV vs CAC
- **规模效应**：边际成本递减吗？

---

## 综合判断

### SWOT 汇总

```
┌─────────────────────┬─────────────────────┐
│  S 优势              │  W 劣势              │
│  产品做得比所有竞品   │  产品明显的短板       │
│  好的 1-2 点         │  1-2 点              │
├─────────────────────┼─────────────────────┤
│  O 机会              │  T 威胁              │
│  市场或技术上的       │  竞品动向或行业变化   │
│  外部利好            │  带来的风险           │
└─────────────────────┴─────────────────────┘
```

### 评分标准（带锚点）

| 评级 | 判定条件 |
|------|---------|
| A | 所有核心维度（产品力、体验、商业）都优秀，几乎没有明显短板。教科书级。 |
| A- | 核心场景统治力强，但有 1 个维度（通常是商业化或增长）有明显短板。 |
| B+ | 核心体验好、有独特价值，但 2+ 个维度可优化（如留存不够、壁垒不深）。 |
| B | 解决了真实问题，但体验或商业上有硬伤，用户可能随时切换到替代方案。 |
| B- | 核心体验有缺陷（流程断裂、关键功能缺失），用户完成核心任务有障碍。 |
| C+ | 存在方向性问题（PMF 未验证、商业模式不成立），或已明显衰退。 |

**打分原则**：
- 如果核心场景体验优秀但商业模式未验证 → B+（不是 A-）
- 如果功能完善但用户增长停滞 → B（不是 B+）
- 如果已关停但曾经有亮点 → C+ 或 B-（看衰退原因）

### 改进建议

只选 **1-2 个最值得做的优化点**。每个建议说明：
- 建议是什么
- 为什么是这个问题（证据来自前面分析）
- 预期效果（最好能量化）

禁止列 5 条以上——没有优先级等于没有建议。

**如果是已关停产品**：改进建议改为"死亡原因分析"和"可复用的教训"，不再提改进建议。

---

## 输出格式

### 报告载体

输出一个**单独的自包含 HTML 文件**，采用**全屏流式布局**（不做卡片式，内容直接铺在背景上）。

### HTML 设计规范

**核心原则：根据 Step 0.5 的风格决策，读取对应风格模板，保留其 CSS 变量、字体、布局骨架和组件样式，替换所有内容数据。**

**执行步骤**：
1. 读取 Step 0.5 选定的风格模板 HTML（如 `templates/style-academic.html`）
2. 保留模板的 `<style>` 块（CSS 变量、字体声明、组件样式、响应式断点）
3. 保留模板的 HTML 骨架结构（hero/stat-grid/bar-list/callout/swot 等组件）
4. 替换所有文字内容、数据、图表为当前产品的分析结果
5. 不得保留模板中的演示数据或占位符文本

**禁止**：
- ❌ 不得手动编写 CSS——必须复用模板样式
- ❌ 不得混合不同风格模板的样式（如用 academic 的配色 + dark 的布局）
- ❌ 不得修改模板的颜色变量、字体声明、间距系统
- ❌ 不得添加模板中不存在的 CSS 组件

### 可视化图表标准

读取 `references/chart-catalog.md` 获取：
- 产品分析常用图型映射表（12 种数据形状 → 推荐图型）
- 选型流程（4 步）
- 禁止的图型（断轴柱状图、雷达图、3D、多色相单序列）
- 视觉语法（灰阶、明度即数据）
- SVG 安全规则（ID 唯一前缀、viewBox、系统字体）

**图表嵌入规则**：使用风格模板中预定义的图表组件（`.bar-list` / `.stat-grid` / `.big-stat` 等），不得自创图表样式。

### 模块顺序（根据分级调整）

**S/A 级产品（全量）**：
1. 标题区 + 产品分级标签
2. 产品速览：一句话定位 + 关键数据侧边栏
3. JTBD 分析：核心任务 + 替代方案 + 判断
4. 发展历程（有时序数据时）：水平时间轴 SVG
5. 功能拆解：核心用户流程箭头图 + 1-2 个功能巧思
6. UI 设计分析：定性视觉风格判断 + 交互评估
7. 数据层：指标体系假设
8. 运营增长：AARRR 框架
9. 商业模式：收入 + 壁垒
10. 竞争格局：表格对比
11. SWOT + 评分
12. 改进建议（1-2 条）
13. 文末：数据来源声明

**B 级产品（轻量）**：
1. 标题区
2. 产品速览
3. JTBD 分析
4. 功能拆解（重点）
5. UI 设计分析
6. 商业模式
7. SWOT + 评分
8. 改进建议（1-2 条）

**C 级产品（已关停/衰退）**：
1. 标题区
2. 产品速览
3. 为什么存在（战略层）
4. 为什么失败（死亡原因分析）
5. 可复用的教训
6. 结语

### 禁止事项

- ❌ 不得引用任何外部 CSS/JS/字体文件（必须完全自包含）
- ❌ 不得使用 `<img>` 引用网络图片（除非用户提供截图）
- ❌ 不得引用任何 JS 图表库（Chart.js/ECharts 等需联网，不可靠）
- ❌ 不得使用 `<canvas>` 绘制图表（必须用 SVG 或纯 CSS）
- ❌ 不得罗列功能清单而不加判断
- ❌ 不得建议超过 2 条
- ❌ 不得使用卡片式容器（report-card）——用全屏 section 替代
- ❌ 不得出现重复的 SVG defs ID
- ❌ 不得忽略响应式——必须包含 768px 和 480px 断点
- ❌ 段落内的关键数据必须用风格模板的主色 + 700w 突出（由 CSS 变量控制）
- ❌ 不得编造数据——搜不到就标注"未公开"
- ❌ 不得对 B/C 级产品执行完整六层分析——必须裁剪
- ❌ 不得使用断轴柱状图——柱的契约是长度∝数值，断轴毁契约
- ❌ 不得使用雷达图（除非用户明确要求）——多维对比用表格更诚实
- ❌ 不得用多色相画单序列——单序列用灰阶或单色相明度梯
- ❌ 不得硬套图型——数据形状匹配不上时用表格或文字替代

### 写作风格

- 开头用一句有张力的引语（pullquote），不啰嗦背景
- 每段不超过 5 行
- 关键判断用粗体或 pullquote 突出
- 不要说"这个产品有...功能"，要说"这个功能解决了...问题"
- 语气自信。不要说"可能是因为"，直接说"这是因为"
- 每个大段之间用 `✦ ✦ ✦` 分隔
- 关键数据一律用风格模板的主色 + 700w 突出（由 CSS 变量控制）

---

## 使用流程

1. **产品分级**：判断产品复杂度（S/A/B/C），决定分析深度
2. **视觉风格决策**：根据产品类型选择对应风格模板（Step 0.5）
3. **生命阶段**：判断产品处于探索期/成长期/成熟期/衰退期
4. **信息收集**：读取 `references/search-channels.md`，按渠道矩阵搜索，缺失数据按规则处理
5. **分层分析**：根据分级结果执行对应层级
6. **输出 HTML 报告**：读取选定的风格模板，读取 `references/chart-catalog.md` 选图型，替换内容输出到当前目录
7. **告知用户文件路径**，询问是否需要调整
